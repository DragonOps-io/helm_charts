app:
  namespace: ""
  secret_name: ""
  aws_secret_name: ""
  account: ""
  stack_name: ""
  database_secret_names_to_use:
  pod:
    min:
    max:
    port:
    healthCheckPath:
    initialHealthCheckDelay:
  autoscaling:
    triggers: []  # Custom KEDA triggers (SQS, RPS, etc.)
  vpa:
    enabled: false
    updateMode: "Off"  # "Off" = recommendations only, "Auto" = apply changes
    minAllowed:
      cpu: 50m
      memory: 64Mi
    maxAllowed:
      cpu: 2
      memory: 4Gi
  service:
    formatted_dns_names: ""
    observability:
      enabled:
      endpoint: ""
    name:
    protocol: TCP
    port:
  resources:
    requests:
      memory: "256Mi"
      cpu: "125m"
    limits:
      memory: "256Mi"
      cpu: "125m"

image:
  name:
  tag:
  url: